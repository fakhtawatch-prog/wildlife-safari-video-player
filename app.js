const $=s=>document.querySelector(s);const state={index:null,date:null,i:0,activity:false};
const video=$('#video'), select=$('#dateSelect'), timeline=$('#timeline');
function fmt(sec){sec=Math.max(0,Math.round(sec));const h=Math.floor(sec/3600),m=Math.floor(sec%3600/60),s=sec%60;return h?`${h}h ${m}m ${s}s`:`${m}m ${s}s`}
function fmtTime(iso){return new Date(iso).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false})}
function dayItems(){return state.index?.days?.[state.date]||[]}
function renderDays(){select.innerHTML=Object.keys(state.index.days).sort().map(d=>`<option value="${d}">${new Date(d+'T00:00:00').toLocaleDateString([], {month:'long',day:'numeric'})}</option>`).join('');state.date=new URLSearchParams(location.search).get('date')||Object.keys(state.index.days).sort()[0];if(!state.index.days[state.date])state.date=Object.keys(state.index.days).sort()[0];select.value=state.date;renderDay()}
function renderDay(){const a=dayItems();state.i=0;const total=a.reduce((x,y)=>x+(y.duration||0),0);$('#dayStats').textContent=`${a.length} recordings · ${fmt(total)} footage · ${a.length?fmtTime(a[0].startTime):'—'} → ${a.length?fmtTime(a[a.length-1].startTime):'—'}`;timeline.innerHTML='';a.forEach((x,i)=>{const d=new Date(x.startTime), start=d.getHours()*3600+d.getMinutes()*60+d.getSeconds(), left=start/86400*100, width=Math.max((x.duration||1)/86400*100,.18);const b=document.createElement('button');b.className='clip';b.style.left=left+'%';b.style.width=width+'%';b.title=`${fmtTime(x.startTime)} · ${fmt(x.duration||0)}`;b.dataset.i=i;b.onclick=()=>load(i,true);timeline.appendChild(b)});renderChapters()}
function load(i,play=false){const a=dayItems();if(!a[i])return;state.i=i;document.querySelectorAll('.clip').forEach((e,j)=>e.classList.toggle('active',j===i));video.src=a[i].url;video.load();$('#videoStatus').textContent=`${fmtTime(a[i].startTime)} · ${a[i].name}`;if(play)video.play().catch(()=>{})}
video.addEventListener('ended',()=>{if(state.i<dayItems().length-1)load(state.i+1,true)});
video.addEventListener('volumechange',()=>$('#videoVolume').value=video.volume);$('#videoVolume').oninput=e=>video.volume=+e.target.value;
$('#nextBtn').onclick=()=>load(Math.min(state.i+1,dayItems().length-1),true);$('#prevBtn').onclick=()=>load(Math.max(state.i-1,0),true);
select.onchange=()=>{state.date=select.value;history.replaceState(null,'','?date='+state.date);renderDay();load(0)};
$('#activityBtn').onclick=()=>{state.activity=!state.activity;timeline.classList.toggle('activity',state.activity);$('#activityBtn').textContent=state.activity?'Full 24-hour View':'Zoom to Activity';if(state.activity){const a=dayItems();if(a.length){const starts=a.map(x=>{const d=new Date(x.startTime);return d.getHours()*3600+d.getMinutes()*60+d.getSeconds()});const ends=a.map((x,j)=>starts[j]+(x.duration||1));const lo=Math.max(0,Math.min(...starts)-1800),hi=Math.min(86400,Math.max(...ends)+1800);timeline.querySelectorAll('.clip').forEach((b,j)=>{const x=a[j],d=new Date(x.startTime),s=d.getHours()*3600+d.getMinutes()*60+d.getSeconds();b.style.left=((s-lo)/(hi-lo)*100)+'%';b.style.width=Math.max((x.duration||1)/(hi-lo)*100,.7)+'%'});timeline.dataset.window=`${lo}-${hi}`}}else renderDay()};
$('#timelineBtn').onclick=()=>{const sec=$('#timelineSection');const hidden=sec.style.display==='none';sec.style.display=hidden?'':'none';$('#timelineBtn').textContent=hidden?'Hide Timeline':'Show Timeline'};
$('#fullscreenBtn').onclick=()=>video.requestFullscreen?.();document.addEventListener('keydown',e=>{if(['INPUT','SELECT','BUTTON'].includes(document.activeElement.tagName))return;if(e.code==='Space'){e.preventDefault();video.paused?video.play():video.pause()}if(e.key==='ArrowLeft')video.currentTime=Math.max(0,video.currentTime-5);if(e.key==='ArrowRight')video.currentTime+=5;if(e.key==='ArrowUp')load(Math.max(0,state.i-1),true);if(e.key==='ArrowDown')load(Math.min(dayItems().length-1,state.i+1),true);if(e.key.toLowerCase()==='f')$('#fullscreenBtn').click();if(e.key.toLowerCase()==='m')video.muted=!video.muted});
function renderChapters(){const c=$('#chapters');c.innerHTML='<p style="color:#888">Chapters are ready for publication storage. Add/edit/delete UI will use stable recording IDs and timestamps.</p>'}
fetch('data/wildlife-index.json').then(r=>r.json()).then(x=>{state.index=x;renderDays();load(0)}).catch(e=>{$('#videoStatus').textContent='Unable to load metadata index.';console.error(e)});
